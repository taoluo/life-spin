# Foam / LifeLoop capability matrix

Current as of 2026-09-13. “Verified” means the named behavior has executable or
source-level evidence; accepting an SB declaration is not counted as runtime parity.

## General notes

| Capability | Owner | Evidence and boundary |
|---|---|---|
| Wiki links, completion, definitions | Foam | Real VS Code + Foam 0.44.6 resolves and completes ordinary links. LifeLoop registers no competing provider. |
| Backlinks / outgoing links | Foam | Foam 0.44.6 contributes the Connections views. LifeLoop's separate Linked Tasks view aggregates task-level inherited links. |
| Tags and page graph | Foam | Foam manifest and bundle contribute Tag Explorer/search and graph commands. These are browsing features; LifeLoop keeps object-level inherited tags for task queries. |
| Page queries | Foam | Real-host list and table queries return the expected page and compose with LifeLoop preview hooks in both extension orders. Bundle inspection confirms tag/link/path/property filters plus list/table/count output. |
| Smart Folders | Foam | Foam contributes `.foam/queries/*.yaml` discovery and Smart Folder commands. Saved-query UI behavior is owned by Foam; LifeLoop has no parallel folder engine. |
| Daily notes and ordinary templates | Foam | The converted LifeLoop daily template is exercised through `foam-vscode.open-daily-note`; Foam expands its date variables. All four converted templates have parsed YAML frontmatter and no SB cursor marker. |
| Page / heading embeds | Foam | Real host renders a Foam embed beside a LifeLoop query in either plugin order. Inner LifeLoop execution inside an embed is not promised. |
| File rename | Foam / VS Code | Ordinary file rename, reference update, heading suffix and dirty source preservation pass in Foam 0.44.6. |
| Folder rename | Foam limitation | Released Foam rewrites links but then loses definition targets for moved descendants. The failure is reproducible; a copied diagnostic patch proves the likely missing refresh. LifeLoop does not add a rename engine. |
| Search, Explorer, Outline, Git | VS Code | Native host features; no LifeLoop duplicate. |

## LifeLoop task contract

| Capability | Status | Evidence and boundary |
|---|---|---|
| Task extraction and context | Verified | Headless tests cover nested context, inherited tags/links, comments and SB-compatible objects. Three live-SB checks explicitly skip when no RuntimeAPI client is available. |
| SourceHandle and mutation validation | Verified | Exact task line/state receipt, duplicate-anchor refusal, same-version body/JSON reads, dirty-buffer writes and CRLF source offsets have regression coverage. Delayed tree/prompt/bridge actions require a guarded handle; page creation records expected absence. |
| Multi-character states | Verified on core, VS Code, CLI and bridge | Full names are retained; explicit `done` policy controls extraction and mutation. Unknown states refuse. Configuration changes, Lua declaration deletion and dirty declarations pass real-host tests. CLI uses explicit `--task-states`; it does not implicitly read VS Code/Lua configuration. Failed or changing policy transitions cannot certify or publish a mixed index. |
| Capture / Process / Today / Waiting / Projects | Verified; current task-only GUI K0 | Filesystem integration composes capture → process → schedule → complete → review. Capture Here is a named guarded mutation; global Inbox processing advances only after a successful action. Today separates overdue deadlines, due dates, current schedules and unfinished earlier schedules. Add from Backlog and Waiting → Next Action keep their user intents separate. In the current isolated task-only host, Capture and Capture Selection wrote to Inbox while retaining editor B, its cursor/selection and the explicit TreeView target; Escape from a populated Capture input wrote nothing. Waiting → Next Action created a guarded child only under the selected Waiting task and returned to editor B. |
| Plan Today / close today and plan tomorrow | Implemented; automated and task-only/Foam GUI-host K0 | One continuous native picker combines existing Today, Backlog and guarded task actions. The evening mode shows factual completions and remaining work, then tomorrow's constraints and candidates. Both planning modes open the Focus note for the planning date's actual week without closing the picker; automated coverage verifies cross-week selection and restoration of the query and selected result. On 2026-09-14, isolated task-only and Foam 0.44.6 hosts both opened `Weekly/2026-09-14.md` and retained the planning picker, scope and long-title task selection; Problems remained 0. Earlier current-host passes also verified Escape without writing, explicit scheduling for tomorrow, refresh and return. macOS Simplified Pinyin composition produced `ming tian`, accepted `明天`, and rapid Return/Escape caused no mutation. No Start/Stop state, execution order or automatic rollover is stored. |
| Task Actions / source peek | Verified in real VS Code | One Quick Pick dispatches existing named commands. Its candidate menu uses the signed current task snapshot and no longer waits for a full task-state refresh; every chosen mutation still performs fresh validation. A cold task-only host accepted the complete immediate input `Open Relat` in the picker without writing any prefix into editor B. Peek Source uses `editor.action.peekLocations`; no Webview or second editor is maintained. |
| Find / Explain / continuous preview | Implemented; Foam GUI-host K0 verified | Find Task supports Open/Completed/Waiting/Someday/All scopes, session-only query/selection return and an optional focus-preserving selection preview. Preview generation guards prevent a slower prior selection from replacing the latest preview; mutation still uses the selected guarded handle. Under Foam 0.44.6, keyboard preview followed the latest selection; cancel plus Return to Last Find restored the query and exact handle. A narrow-window long-title pass retained identification, preview and Task Actions access. Find itself has not repeated the planning picker's Simplified Pinyin run. |
| Project page-scoped gaps and actions | Verified automatically and in current task-only/Foam GUI hosts | Active Project signals distinguish no open task, all Waiting and no actionable task on that project page. Paused projects have no action-gap. Stable project nodes provide Preview/Open/Add Next Action/Pause/Leave Unchanged; Add and Pause revalidate a fresh page snapshot. Preview reuses the read-only resumption result instead of showing only the source page, and Live Review restores its scope, query and selected Project afterward. On 2026-09-14, isolated task-only and Foam 0.44.6 hosts displayed separate “Open tasks on this project page” and “Related open tasks from other pages” groups, the membership caveat, and returned to the Project source; Problems remained 0 and Foam added no provider error. Task-only Add Next Action updated only the selected Project. |
| Project resumption and closure facts | Implemented; automated and task-only GUI-host K0 | The read-only Project Preview / resumption result separates open tasks on the Project page from related tasks on other pages, recent dated completions and known Reminder/Calendar bindings. Task-only previously opened a paused-project brief through `lifeloop-result:` and the `completed` path displayed the narrow closure counts; Escape left the Project active. Choosing `completed` or `archived` still requires a fresh guarded Project status write; tasks and external objects remain unchanged. |
| Add / Open Related Page | Add is verified automatically and in the current task-only GUI host; Open is verified automatically and in current task-only/Foam GUI hosts, including full Enter/Escape and narrow-window K0 | A selected task can add one direct link to an existing Project, Person or Note after task and target-page validation. Task Actions conditionally opens directly linked Person or Note pages, showing type and full path when a choice is needed. Current task-only and Foam 0.44.6 hosts confirmed the explicit task remained A while the editor showed B, Project and B links were excluded, the exact Note opened, Go Back returned to B and Problems remained 0. The narrow-window pass retained long-path identification. A fresh task-only host used Return to enter the related-page picker and open the exact long-path Note, Escape to cancel without changing B, and `Ctrl+-` to return to B; the TreeView target remained A and B stayed at line 1, column 1. Project navigation remains the existing Open Project action. The feature does not infer inherited or similar links, create or move a page, change project ownership, Process Inbox or create an Interaction. |
| Linked Tasks | Verified | Uses task-level inherited links, carries a guarded SourceHandle, and explains direct versus inherited relations without replacing Foam backlinks. |
| Personal relationship loop | Verified headless and in real VS Code/Foam | `tags: person` pages stay ordinary Foam pages. Explicit dated Journal interactions derive last contact and reconnect signals; follow-ups are ordinary guarded tasks. Meeting Wrap-up is implemented for an exact Calendar-bound task with direct Person links and lets the user independently choose Log Interaction, Add follow-up and Complete. Its new combined picker has automated verification; real-host K0 is pending. Mentions never count as contact, and no CRM database, score or dashboard is maintained. |
| Completion history / reopen | Verified | Completion dates are written or removed by named mutations; externally reported completion dates are preserved by the bridge. |
| Review / freeze / weekly focus | Implemented; automated and GUI-host verification | Review interpolation is retained and a frozen snapshot remains unchanged after later vault edits. A live Review marker opens one fresh-query session with Open tasks, Active projects, Waiting, Someday, Completed this week, Unscheduled and Paused scopes; supported actions refresh in place and preserve a reasonable selection. Foam 0.44.6 verified the existing continuous Review interactions. In the current task-only host, clearing one direct `#waiting` item removed it and its inherited child from the Waiting scope, kept the same Review picker open, preserved `Scope: Waiting`, selected the remaining adjacent Waiting item and left editor B unchanged. The direct tag command now also reports when removing a direct parked tag leaves the same effective tag inherited; this increment is automated only. Task-only previously opened the ordinary next-week focus note at `Weekly/2026-09-14.md` with Focus, Related Projects / Pages and Not This Week sections and no copied task checkboxes. Frozen Review text remains navigation-only. |
| Review period facts | Implemented; automated and task-only GUI-host K0 | A read-only weekly report includes tasks with completion dates in the current Monday–Sunday range and explicit interactions on reliably dated Journal pages. Task-only opened the current-week `lifeloop-result:` report and showed the fixture's dated completion. It explicitly omits task creation and Inbox processing because those facts have no reliable timestamps. |
| Baking | Verified core | Bake/update/unbake tests cover dynamic results becoming Markdown. This is not a general publishing framework. |
| Apple Notes full-body import | Verified with fakes; real Notes pending | A whole plain-text body is one pending Inbox item: first line plus indented continuation lines. Rich content is read without claiming a lossless Markdown round trip. |
| Apple Notes pending sync | Verified with fakes; real Notes pending | Durable three-way baselines propagate one-sided plain-text edits. Concurrent edits pause with the binding intact and enter Resolve; rich Notes cannot be overwritten. Processing still hands ownership to Markdown. |
| Reminders / Calendar | Verified with fakes; real apps pending | Reminders retain completion reconciliation. Calendar shares only the task/event title; Calendar owns time, recurrence, attendees and location. Calendar writes use external compare-and-set and creation compensation. Divergence keeps the binding and pauses for Resolve. |
| External binding display/actions | Verified in real VS Code before latest UX pass | Exact task attributes receive lightweight 🔔/📅 decorations and hover Sync/Open/Detach/Copy actions. A native Output Channel records failures and conflicts. External systems only surface divergence; LifeLoop's VS Code Resolve command is the sole resolution control plane. It offers Use Markdown, Use Apple, or explicit Detach and refuses a choice if either side changed after it was shown. Cross-window writer admission remains a separate safety milestone. |
| Read-only derived documents | Verified in task-only and Foam hosts | Query Result and Pre-meeting Brief use the `lifeloop-result:` provider, remain outside the vault/index and retain bounded source navigation. Project Resumption and Review Period Facts use the same provider and were opened in the current task-only host. Review and Resolve have not been automatically migrated. |
| X-Ray | Verified | Uses JSON to explain LifeLoop objects; it is not a general inspector. |

## SilverBullet script compatibility

Official examples are retained by default. Trial absence is not a deletion criterion.
The host remains bounded: arbitrary JavaScript import, shell, network and unnamed page writes
are not enabled merely for parity because they bypass LifeLoop's mutation and ownership contract.

| API | Current support | Current implementation boundary |
|---|---|---|
| Lua language, SLIQ, `index.*`, `tags.*`, read-only `space.*` | Implemented | Official docs execute through the host; supported query and rendering paths have focused tests. |
| `command.define` | Adapted | Implemented through VS Code Quick Pick. Callback and cancellation are tested; SB keybinding metadata is not reproduced. |
| `taskState.define` | Implemented | Full state names, ordering, done policy and live invalidation are wired through production paths. |
| `actionButton.define` | Adapted | Implemented as VS Code status-bar commands and used by repository fixtures. |
| `space-style` | Adapted | Implemented as filtered, scoped Markdown-preview CSS; unrestricted editor CSS is absent. |
| `widget.*` / `dom.*` | Partial adaptation | Inline Markdown and allowlisted widget content render in preview; arbitrary DOM access, docks and refresh orchestration are absent. |
| `event.listen` / `event.dispatch` | Declaration compatibility only | Listener declarations are preserved and unsupported names are reportable; listeners are not fired by a general event bus. |
| `mq.*` | In-memory primitives only | Send, batch send, depth and subscription declarations exist; persistent delivery and a generic consumer loop are absent. |
| `service.define` | Declaration compatibility only | Definitions remain available to parsing and inspection; no background service supervisor runs them. |
| `syntax.define` | Declaration and matcher only | Definitions and safe matching exist; editor rendering and highlighting are not implemented. |
| SB `$anchor` / `page@…` refs | Explicit navigation | `lifeloop.openSbRef` resolves exact anchors/positions and refuses ambiguous/missing targets without creating files. Foam ordinary clicks may still offer a `Page@anchor` note; special-ref rename is not transactional. |

## Current verification

- `npm run verify`: typecheck, vendor guard and schema guard pass; 65 test files and 910 tests pass; 3 live-SB checks skip because no RuntimeAPI client answered.
- Focused VS Code command verification: 51 tests pass.
- Focused current-slice verification: 3 files and 143 tests pass for weekly Focus return, Project Preview, read-only focus preservation and inherited-state feedback.
- Live SilverBullet 2.10.0: current result is SKIP. The isolated fixture and checks remain available, but no live RuntimeAPI client answered this run.
- Latest VS Code 1.136.1 background integration runner still aborts before extension loading in AppKit `_RegisterApplication` with `SIGABRT`; the crash report contains no LifeLoop stack. A GUI-launched Extension Development Host bypassed that runner failure. In task-only it verified the three Project signal labels, Add Next Action targeting, direct Add Related Link with typed/full-path candidates and zero Problems. With Foam 0.44.6 it verified the same Project signals plus focus-preserving Preview/close with the Project row still selected and zero Problems.
- Current Foam GUI-host K0 verified Live Review mutation-and-refresh continuation, adjacent selection, rapid Return/Escape, Find keyboard preview, cancel/return restoration, narrow-window/long-title handling and the evening planning mutation/return path. Current task-only K0 verified Plan Today scope/cancel, the complete review-today → plan-tomorrow flow, an explicit saved schedule mutation, Project Resumption, Project closure-check cancellation and Review Period Facts. macOS Simplified Pinyin was switched on at the operating-system level; per-key input composed `ming tian`, Space accepted `明天`, and rapid Return/Escape returned without mutation. This IME evidence applies to the native planning QuickPick, not the managed-field suggestion widget.
- The running ordinary VS Code window used an earlier installed contribution manifest, so reloading the newly built source did not expose the latest Plan Today entry. The new Focus-return and combined Project Preview paths remain pending in task-only and Foam hosts; no extension was installed and no extra extension-host window was opened for this check.
- Earlier working-tree host evidence remains 27 task-only tests passed with 8 Foam-only skips, and 34 Foam 0.44.6 tests passed with 1 known folder-rename skip. It predates the latest in-place UX slice.
- Real Apple Notes and Calendar: SKIP because AppleEvent calls timed out on this host. Fake bridges verify Notes full-body import, pending two-way sync, Calendar compensation, all three conflict paths and stale-resolution refusal. Reminders uses the same fake-backed verification; no personal external data was changed.
- Original trial and official sample sources remain unchanged; inventories use durable isolated copies.
